<?php
// Obtém a URL para decidir qual CSS carregar
/*$url = isset($_GET['url']) ? explode('/', rtrim($_GET['url'], '/')) : [''];
$currentPage = end($url);

if ($currentPage === 'login' || $currentPage === 'logar') {
    // Carrega o CSS específico para a página de login
    echo '<link rel="stylesheet" href="' . DIRCSSADMIN . '/login.css">';
} else {
    // Carrega o CSS para o dashboard principal
    echo '<link rel="stylesheet" href="' . DIRCSSADMIN . '/dashboard.css">';
}*/
?>

<meta name="sistema" content="Dashboard Administrativa">